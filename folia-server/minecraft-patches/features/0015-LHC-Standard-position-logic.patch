From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: ShyAlly <1447610+ShyAlly@users.noreply.github.com>
Date: Fri, 12 Dec 2025 14:22:23 -0500
Subject: [PATCH] LHC - Standard position logic


diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index c18f55f60be88a9c196c48ea22d4e0de5ff4a252..03bf18431a1127841942cd2518203ad4c311414f 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -1,5 +1,6 @@
 package net.minecraft.world.entity;
 
+import com.github.shyally.RandomEndSpawnFinder;
 import com.google.common.collect.ImmutableList;
 import com.google.common.collect.Lists;
 import com.google.common.collect.Sets;
@@ -4721,88 +4722,13 @@ public abstract class Entity implements SyncedDataHolder, Nameable, EntityAccess
             portalInfoCompletable = new ca.spottedleaf.concurrentutil.completable.CallbackCompletable<>();
 
             portalInfoCompletable.addWaiter((TeleportTransition info, Throwable throwable) -> {
-                BlockPos center = new BlockPos(0, 0, 0);
-                final int radius = 80;
-
-                destination.moonrise$loadChunksAsync(
-                    center,
-                    radius + 16,
-                    ca.spottedleaf.concurrentutil.util.Priority.HIGH,
-                    (chunks) -> {
-                        var random = ThreadLocalRandom.current();
-
-                        // Limit attempts in case someone does something silly
-                        // If it fails we'll end up at the regular end spawn
-                        BlockPos finalPosition = null;
-                        for (int i = 0; i < radius * 4; i++) {
-                            BlockPos randomPositionYZero = new BlockPos(center.getX() + random.nextInt(radius * 2) - radius, center.getY(), center.getZ() + random.nextInt(radius * 2) - radius);
-                            BlockPos potentialPosition = destination.getHeightmapPos(Heightmap.Types.WORLD_SURFACE, randomPositionYZero);
-                            if (potentialPosition.getY() > 0) {
-                                potentialPosition = potentialPosition.atY(potentialPosition.getY() - 1);
-
-                                BlockState landingBlock = destination.getBlockStateIfLoaded(potentialPosition);
-                                if (landingBlock == null) {
-                                    LOGGER.info("Random end spawn - Potential location wasn't loaded?");
-                                    continue;
-                                }
-                                if (!landingBlock.isSolidRender()) {
-                                    continue;
-                                }
-
-                                // Yay we found one
-                                finalPosition = potentialPosition;
-                                break;
-                            }
-                        }
-
-                        if (finalPosition == null) {
-                            LOGGER.info("Random end spawn - Failed to find a suitable spot");
-                            // Nothing to do, rip
-                            originalPortalInfoCompletable.complete(info);
-                            return;
-                        }
-
-                        float yRot;
-                        if (finalPosition.getX() == 0) {
-                            if (finalPosition.getZ() < 0) {
-                                yRot = Direction.SOUTH.toYRot();
-                            } else {
-                                yRot = Direction.NORTH.toYRot();
-                            }
-                        } else if (finalPosition.getZ() == 0) {
-                            if (finalPosition.getX() < 0) {
-                                yRot = Direction.EAST.toYRot();
-                            } else {
-                                yRot = Direction.WEST.toYRot();
-                            }
-                        } else {
-                            double degrees = Math.toDegrees(Math.atan2(finalPosition.getZ() * -1, finalPosition.getX() * -1));
-                            yRot = (float)(degrees - 90);
-                            if (yRot > 180) {
-                                yRot -= 360;
-                            }
-                            if (yRot < -180) {
-                                yRot += 360;
-                            }
-                        }
-
-                        Vec3 playerPosition = finalPosition.getCenter().add(0, 0.6, 0);
-                        LOGGER.info("Random end spawn - calculated position: {} facing {}", playerPosition, yRot);
-
-                        originalPortalInfoCompletable.complete(
-                            new TeleportTransition(
-                                destination,
-                                playerPosition,
-                                info.deltaMovement(),
-                                yRot,
-                                info.xRot(),
-                                info.relatives(),
-                                info.postTeleportTransition(),
-                                info.cause()
-                            )
-                        );
+                RandomEndSpawnFinder.asyncFindRandomEndSpawn(destination, (spawn) -> {
+                    TeleportTransition newTransition = info;
+                    if (spawn != null) {
+                        newTransition = info.withPosition(spawn.position).withRotation(spawn.yRot, info.xRot());
                     }
-                );
+                    originalPortalInfoCompletable.complete(newTransition);
+                });
             });
         }
         // LHC End
